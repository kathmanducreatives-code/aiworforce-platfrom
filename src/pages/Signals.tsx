import SignalFeed from "@/components/signals/SignalFeed";

// Phase 5 — Signal Feed / Memory Workbench page. Shows persistent memory
// (signals, drafts, saved outputs) across conversations.
export default function Signals() {
  return <SignalFeed />;
}
