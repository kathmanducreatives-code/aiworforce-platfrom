# Company-details live schema probe — `apify_linkedin_company_details`

**Verdict: PASS — LIVE COMPANY SCHEMA VERIFIED**

One-company live schema probe of the structured company-enrichment actor against the
merged Find Leads Phase 2 code, run locally through the **real** merged tool path with
a no-op database client (zero writes). Not a Find Leads workflow, not Q1.

## Release context

| Item | Value |
|---|---|
| Merged main SHA | `8ae6b150` (PR #47 merge commit; contains `8f86d70a`) |
| run-agent version (TEST) | **v82** — NOT yet redeployed (deploy pending `SUPABASE_ACCESS_TOKEN`) |
| orchestrate version (TEST) | v31 (unchanged) |
| TEST project | `zbwsbnqqpkvdhqwavjke` (MCP server binding verified via get_project_url) |
| Production | `wqnigjhcwjxtmordrwno` — NOT accessed |

## Actor identity (canonical, immutable)

| Field | Value |
|---|---|
| actor_key | `apify_linkedin_company_details` |
| actor_id | `harvestapi/linkedin-company` |
| actor_output_type | `company_details` |
| provider | apify |

## Sanitized input (deterministic builder shape)

```json
{ "companies": ["https://www.linkedin.com/company/actively-ai"], "max_results": 1 }
```

Company `actively-ai` chosen from an existing TEST `lead_candidates.company_linkedin_url`
(2 founders); URL canonicalized (tracking params stripped) by the input normalizer.

## Run facts

| Item | Value |
|---|---|
| provider_run_id | `CfHNWy989cfETIhy8` |
| runtime | ~21.8 s |
| cost | not surfaced by the actor response |
| dataset item count | 1 |
| typed `company_items` count | **1** |
| legacy `items` present | 0 |
| truncation detected | **no** |
| outcome | `enriched` |

## Result-path integrity

- **Typed `company_items` path used** — the item arrived complete; `legacy_items[]` was empty, so no job-normalization path was taken.
- **No `provider_payload` wrapping**, **no 4,000-char truncation**, **no JSON-preview truncation**.
- Complete item reached `company_items[0]` → `interpretCompanyActorResponse` → normalized evidence.

## Live top-level keys (names only — no values)

```
active, autoGenerated, backgroundCover, backgroundCovers, callToActionUrl, companyType,
description, employeeCount, employeeCountRange, followerCount, fundingData, id, industries,
jobSearchUrl, linkedinUrl, locations, logo, logos, name, originalQuery, pageType,
pageVerified, peopleStats, requestId, similarOrganizations, specialities, universalName, website
```

## Normalized firmographics (no PII)

| Field | Value | Normalized? |
|---|---|---|
| name | Actively | yes |
| linkedinUrl | https://www.linkedin.com/company/actively-ai | yes (canonical) |
| universalName | present | yes |
| officialWebsite | https://www.actively.ai | yes (query/fragment stripped) |
| industries | ["Software Development"] | yes (array) |
| companyType | Privately Held | yes |
| employeeCount | 82 | yes |
| employeeCountRange | { start: 51, end: 200 } | yes |
| headquarters | { country: United States, countryCode: US, region: NY, city: New York City } | yes (headquarter marker + parsed) |
| description | present → `company_business_model` (confidence **low**) | yes |
| foundedYear | — | not returned by actor for this company |
| tagline | — | not returned by actor for this company |

Evidence mapped (6): `company_identity` (high), `company_website` (high), `company_industry` (high),
`company_business_model` (low), `company_size` (medium), `company_geography` (medium) — all with
provenance `provider=apify / actor_key=apify_linkedin_company_details / actor_id=harvestapi/linkedin-company / verified=true`.

## Fixture comparison vs `linkedinCompanyActorFixture.ts`

| Expected field | Classification |
|---|---|
| name, linkedinUrl, universalName, website, industries, description, companyType | exact match |
| employeeCount, employeeCountRange (`{start,end}`) | exact match |
| locations → headquarters (`headquarter` marker + `parsed`) | exact match |
| specialities (actor spelling `specialities`) | supported (present; not mapped to evidence) |
| tagline | absent but optional (not returned for this company) |
| foundedOn / foundedYear | absent but optional — **live shape UNVERIFIED** (not returned for this company) |
| phone | absent in this live item (fixture carries a synthetic phone only to prove redaction) |

Extra live keys not in the fixture (ignored by the tolerant normalizer, none sensitive):
`active, autoGenerated, backgroundCover(s), callToActionUrl, fundingData, jobSearchUrl, logo(s),
originalQuery, pageType, pageVerified, peopleStats, requestId, similarOrganizations`.

**No unsupported live variations. No normalization defects.**

## Sanitization check

- `raw_contains_phone_key`: false · `raw_contains_email_key`: false
- `evidence_json_has_phone`: false
- Raw payload NOT copied into evidence/observability; only normalized fields leave the normalizer.
- Report contains no phone/email/tokens/authorization headers/raw complete payload.

## Database deltas (TEST, before vs after)

| Table | Before | After | Δ |
|---|---|---|---|
| lead_candidates | 426 | 426 | 0 |
| contacts | 165 | 165 | 0 |
| signals | 426 | 426 | 0 |
| accounts | 149 | 149 | 0 |
| outreach_drafts | 64 | 64 | 0 |
| approvals | 25 | 25 | 0 |
| task_plans | 181 | 181 | 0 |
| tasks | 353 | 353 | 0 |
| Penn tasks | 24 | 24 | 0 |
| activity_feed | 1640 | 1640 | 0 |

Company Brain fingerprint `4fc444f7f8f05b18644be7e9219d7240` — unchanged. No Aria task, no Find Leads plan.
(The probe used a no-op DB client, so `tool_calls` also received no probe row.)

## Verdict & next step

**PASS — LIVE COMPANY SCHEMA VERIFIED.** The live `harvestapi/linkedin-company` result reaches
`company_items[0]` complete, un-truncated, and un-job-normalized; website/industry/size/geography/
identity/business-model normalize correctly; sensitive fields excluded; zero database side effects.

Minor honest gap: `foundedOn` and `tagline` were not returned for `actively-ai`, so their live shapes
remain unverified against the fixture (both optional; normalizer tolerates absence). A future probe on a
company that publishes a founded year would confirm the `foundedOn.year` path.

**Exact next step:** provide `SUPABASE_ACCESS_TOKEN` so run-agent is deployed to TEST
(`supabase functions deploy run-agent --project-ref zbwsbnqqpkvdhqwavjke`, v82 → v83, orchestrate stays
v31), then run boot checks; Q1 remains deferred to a later session. Rotate the Apify token used for this
probe.
